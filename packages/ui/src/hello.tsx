import { AppBar, Box, Toolbar } from "@mui/material";

export const Hello = () => {
  return (
    <AppBar>
      <Box>
        <Toolbar disableGutters>Hello</Toolbar>
      </Box>
    </AppBar>
  );
};
