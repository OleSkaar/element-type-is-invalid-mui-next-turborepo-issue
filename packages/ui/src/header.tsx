import { AppBar, Box, Toolbar } from "@mui/material";

export const Header = () => {
  return (
    <AppBar>
      <Box>
        <Toolbar disableGutters>Header</Toolbar>
      </Box>
    </AppBar>
  );
};
